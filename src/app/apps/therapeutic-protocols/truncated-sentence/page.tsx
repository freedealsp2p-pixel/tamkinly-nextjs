import TruncatedSentenceClient from './TruncatedSentenceClient';

/**
 * Truncated Sentence — FREE therapeutic exercise.
 * No access gate: the technique itself is completely free,
 * listed alongside the paid protocols.
 */
export const dynamic = 'force-dynamic';

export default function TruncatedSentencePage() {
  return <TruncatedSentenceClient />;
}
